# Meta Y Agent Harness Implementation Ledger

This is the single execution ledger for the Agent Harness work. `PROJECT_BINDING_CHAT_CRON_PLAN_V2.md` is frozen as historical input; unfinished items from that plan are tracked here under the relevant phases.

## Baseline

- Branch baseline: `dev-local...upstream/main [ahead 4]`
- Implementation-start dirty worktree: none observed by `git status --short --branch`
- Guardrail: do not revert or overwrite unrelated changes if new local edits appear while this ledger is active.

## Legacy Plan Reconciliation

| Legacy item | New phase | Status | Notes |
| --- | --- | --- | --- |
| Project changes local resource locator | Phase 4 | done | Project insight now resolves the nearest project root from the current working directory using manifest/git markers and surfaces it in the Chat Project panel. |
| Locate current project, modified files, and new files | Phase 4 | done | Project insight now returns git-scoped changed files for the located project so the Project panel can show added/modified/deleted/untracked files even before a reply-level review summary exists. |
| Shared project understanding memory per workspace | Phase 5 | done | Project insight now shares a workspace + located-project cache entry for structure hints, key files and command guidance, then rehydrates the current working directory on each request so the same project context can be reused across consecutive turns. |
| Reduce repeated directory scans/file reads | Phase 5 | done | Project structure snapshots now use lightweight root/marker fingerprints for invalidation, so repeated requests reuse cached stack/module/command hints while git-scoped changed files stay fresh per request. |
| Workspace boundary, recovery logic, and access wording | Phase 3 / Phase 4 | done | Approval findings now preserve workspace-policy metadata through to Chat and Security Audit, pending-approval cards explain boundary/policy causes plus recovery hints, and Workspace Policy screens now clarify that full project permission never bypasses workspace roots / allowed / denied path rules. |
| Continuous status update mechanism | Phase 0 | in_progress | This ledger is now the canonical status surface. |

## Built-in Template Requirement Intake

| Requirement source | Template | Target phases | Status | Landing notes |
| --- | --- | --- | --- | --- |
| `docs/agent-template-codex.md` | `builtin.coding_agent` / 编码工程助手 | Phase 4 / Phase 5 / Phase 6 | in_progress | Phase 4 owns workspace policy, approval, coding UI and diff/test surface; Phase 5 owns project cache and context routing; Phase 6 owns AgentProfile, CapabilityPack and system template packaging. Initial JSON seed is now under `mateclaw-server/src/main/resources/templates/coding-agent.json`. |
| `docs/agent-templaite-teacher.md` | `builtin.teacher_exam_assistant` / 名师出题助手 | Phase 5 / Phase 6 | in_progress | Phase 5 owns knowledge/session context routing; Phase 6 owns education DomainTemplate, AgentProfile, CapabilityPack, knowledge binding declarations and mock acceptance tasks. Initial JSON seed is now under `mateclaw-server/src/main/resources/templates/teacher-exam-assistant.json`. |

## Current Delivery Priorities

| Priority | Status | Scope | Landing rule |
| --- | --- | --- | --- |
| P0 - Local Agent foundation | in_progress | Define and land a Codex/OpenClaw-style local-agent foundation for desktop coding workflows: local file/git/search/run execution, minimal-context bridging to remote models, workspace-boundary enforcement, approval/sandbox controls, and project-aware context reduction instead of full directory upload. | This is now the highest-priority architecture track for coding scenarios and must be settled before expanding more remote-only coding flows or additional industry packs. |
| P0 - Core business path | in_progress | Make the current Agent Harness usable through complete Agent creation, knowledge binding, chat execution, context routing, tool policy, and visible run state. | Do not expand new industries until the two built-in Agent cases are runnable end to end. |
| P1 - Built-in Agent application cases | in_progress | Complete `builtin.coding_agent` and `builtin.teacher_exam_assistant` with default Agent data, default Wiki/knowledge seeds, memory defaults, declared skills/tools, template policy, and mock acceptance tasks. | These two templates are the acceptance baseline for the whole product architecture. |
| P2 - Chat attachments and large context | pending | Reference `https://github.com/lintsinghua/claude-code-book` for Agent Harness context compression, memory, tool permission, and multimodal/chat loop patterns; optimize image chat, file attachments, large attachment indexing, and context-budget routing. | Start after P1 creates complete runnable cases, so attachment/context behavior can be verified in real workflows. |
| P3 - Channel/Cron/plugin convergence | deferred | Phase 7 channel, Cron, plugin marketplace, health checks, and capability declaration convergence. | Resume after core business and two built-in Agent cases are stable. |
| P4 - Additional industry packs | deferred | Estate, agriculture, hotel operations, medical research, manufacturing, broader education packs. | Reuse the same template/profile/capability/knowledge-binding structure after the first two cases are proven. |

## Knowledge / Wiki Business Logic

| Concept | Current behavior | User-facing interaction | Runtime usage |
| --- | --- | --- | --- |
| Knowledge Base | A workspace-level container for source materials, generated pages, processing config, graph view and retrieval index. | Users create/manage KBs in Wiki, upload raw materials, process them into pages, edit pages and inspect graph/config. | KBs are not automatically injected into chat unless an Agent explicitly binds them. |
| Wiki Page | A structured page inside one KB, generated from raw materials or created as a template/default page. | Users read/search/edit pages from the currently selected KB page list; archived pages remain under that KB's archived section instead of mixing with the KB list. System/locked pages remain protected by existing page guards. | Chat uses page summaries/snippets first; full page reads are available through wiki tools when relevant. |
| Agent KB binding | Optional list of KB ids stored on the Agent. Empty list means no persistent wiki context is injected. | Users bind/unbind KBs in Agent create/edit under "会话知识库". | `WikiContextService` and `ContextRouterService` only use KBs explicitly selected on the Agent. |
| Template default KB seed | Built-in templates can create starter KBs/pages as sample/default data. This is not a mandatory binding. | Template cards show default KB seed readiness only when the template declares seed data. | Seeded KBs become normal workspace KBs; users decide whether to bind them to an Agent. |
| System Wiki page | Scaffold/operational page created by the platform (for example overview/log) with protected page guards. | These pages stay inside the KB but are not treated as ordinary business seed content and remain protected from normal delete flows. | Wiki guards/tools can read them when needed, but ordinary business statistics and delete operations should continue to respect the protected page type. |
| Template seed Wiki page | Starter page materialized from a built-in template, typically to provide default rules/examples for a business Agent. | Users see and edit these pages like normal KB content after creation. | They count as normal business knowledge content once seeded, even though the initial source is a template declaration. |

## Unified Product Follow-up Checklist

| Workstream | Status | Scope | Landing notes |
| --- | --- | --- | --- |
| Local agent execution split and desktop bridge | in_progress | Establish a strict split between desktop-local execution and remote-model reasoning for coding workflows, so local directory reading/editing/testing never requires full directory traversal upload. | The target model is: desktop/app hosts local file/git/search/run capabilities and project indexing, server/runtime keeps policy/audit/orchestration state, remote models receive only user prompt + routed project summary + selected file snippets + tool results. This workstream should converge with Phase 4 workspace policy and Phase 5 project cache/router rather than introducing a second parallel runtime. |
| Auth/session persistence and relogin | done | Unify login persistence, token renewal persistence, request token lookup, and logout redirect cleanup under shared auth helpers. | `auth.ts` now exposes `getAuthToken()`, `saveAuthSession(...)`, `clearAuthSession()`, and `logoutAndRedirect(...)`; login, router, layout logout, API requests, and token-renewal handling all consume the same path. |
| Wiki delete confirmation unification | done | Replace browser-native `confirm()` / `alert()` flows in Wiki KB/page delete operations with the product confirm dialog and message feedback. | KB delete, batch delete, and page delete now use `mcConfirm(...)` plus `ElMessage.error(...)`, aligning Wiki destructive actions with the rest of the console UX. |
| Desktop packaged-default behavior | done | Keep dev builds pointed at local backend by default, while packaged desktop builds default to the hosted Meta Y service and remain reconfigurable to local/custom nodes. | `desktopConfig.cjs` now computes packaged-aware defaults; `main.cjs` loads/saves config with `app.isPackaged`; docs/i18n now describe the client-only shell distribution instead of bundled JRE/local backend. |
| Desktop packaged login proxy path | done | Fix packaged desktop login failures when the local shell proxies `/api` requests to a remote hosted backend. | Root cause was the Electron local proxy forwarding browser `Origin` / `Referer` plus prior forwarded-header state to the hosted backend, which triggered remote `Invalid CORS request` 403 responses during `/api/v1/auth/login`. `mateclaw-desktop/electron/localServer.cjs` now strips `Origin`, `Referer`, and forwarded headers for proxied `/api/*` requests; `mateclaw-desktop/electron/desktopConfig.cjs` now auto-migrates packaged configs away from stale local/legacy hosted defaults and clears redundant proxy values that equal the effective backend. Validation reproduced the old 403 through the local desktop proxy, then confirmed the fixed path returns the expected auth-layer response (`200` envelope with invalid-credentials payload for a bad test login). A fresh portable package was rebuilt to `.release/manual-20260519-141645` and `mateclaw-desktop/release/Meta Y Desktop 1.1.137-SNAPSHOT.exe`. |
| Workspace invite/share flow parity | done | Extend existing member-based workspace sharing toward a Codex-style invite flow. | Workspace admins can now generate signed invite links with target roles; recipients can open a dedicated `/workspace-invite` page, preview the workspace/role, sign in with redirect preservation if needed, and accept the invite to join or upgrade into the workspace without entering the admin-only Members screen. |
| Residual logout session-noise cleanup | done | Suppress misleading post-logout session/conversation load toasts while auth redirect is already in progress. | `auth.ts` now exposes `isAuthRedirectInProgress()`, and secondary consumers such as Chat conversation loading and the Sessions view skip load-failure toasts once logout/401 redirect has begun, avoiding misleading “获取会话失败 / Failed to load sessions” noise during sign-out. |
| Chat agent/conversation restore regression | done | Fix workspace Chat entry, Agent switching, deleted conversation fallback, and polling interference with local new-chat drafts. | Chat now filters the side conversation list by the selected Agent, switches to that Agent's preferred conversation when available, opens a new draft when no conversation exists, falls back to the workspace default Agent if the previous Agent was deleted, and preserves local draft conversations while polling refreshes server conversations. |
| Desktop/web sign-in persistence lifetime | done | Make existing token persistence usable across ordinary desktop restarts and multi-day sessions. | The backend JWT default lifetime is now 30 days with a 7-day renewal threshold; the existing frontend `localStorage` token path and response-header renewal continue to provide sign-in persistence without storing plaintext passwords locally. |
| Workspace invite and member permission hardening | done | Replace stateless reusable invite links with DB-backed single-use invite records, and tighten shared Agent/KB modification permissions. | Workspace admins now generate auditable invite records that can be listed and revoked; invite acceptance validates record status, expiry, and use count before adding/upgrading membership. Shared Agent creation/update/template application/bindings and shared Wiki/KB write/config/import operations now require workspace `admin` or `owner`, while members keep read/chat usage and user-isolated conversations. |
| Workspace invite page regression | done | Fix invite landing page text rendering and skip redundant join confirmation for users already in the workspace. | The invite page now reads the correct `security.workspaceInvite` locale path. Invite preview returns `alreadyMember/currentRole`; existing workspace members are switched into the workspace and sent back to Chat without consuming the invite, and direct accept calls by existing members no longer consume a link or upgrade roles. |
| Agent instance chat home configuration | done | Add per-Agent instance homepage configuration for Chat empty-state subtitle and four quick-start entries, with smart fallback when unset. | Agent instances now persist `homeSubtitle` and `homeQuickStartsJson`; Agent edit has a "聊天首页" tab for configuring subtitle plus four quick starts. Chat empty-state uses instance config first, then derives onboarding entries from the current Agent, covering function overview, usage guide, core business task, and typical example. Built-in Teacher and Coding templates include Chinese default homepage content. Verification: `mateclaw-ui/node_modules/.bin/vue-tsc.cmd --noEmit` passed; both built-in template JSON files parsed with PowerShell `ConvertFrom-Json -Encoding utf8`. Backend compile remains blocked by missing `mvn` / Maven wrapper. |
| Server deployment and client packaging handoff | done | Provide a production-oriented Compose deployment path for the backend stack and scripts/docs for customer desktop packaging. | Added `docker/docker-compose.server.yml`, `docker/.env.production.example`, `docker/init-server.sh`, `docker/init-server.ps1`, `docker/package-client.ps1`, and `docker/DEPLOYMENT.md`. The server Compose runs MySQL, SearXNG, and Meta Y Server with production secrets from `.env`; init scripts generate local deployment secrets; the client packaging script injects the customer backend URL during Electron packaging and restores source afterward. Verification: `docker compose --env-file .env.production.example -f docker-compose.server.yml config` passed. |
| Meta Y runtime update and offline images | done | Keep the server test/release path updatable without rebuilding on every jar/static change, and align visible runtime branding with Meta Y. | Added runtime-only compose and scripts: `docker/docker-compose.runtime.yml`, `docker/Dockerfile.server-runtime`, `docker/.dockerignore`, `docker/package-runtime.ps1`, `docker/export-offline-images.sh`, `docker/load-offline-images.sh`. The runtime compose mounts `docker/runtime/app/app.jar` and `docker/runtime/static/`, so future backend/frontend updates can replace files and restart the server container. Test server `192.168.0.50` has been switched to `docker-compose.runtime.yml`, health check returned `UP`, runtime image `yliyunclaw-server-runtime:local` is 287MB, and the offline image package was exported to `/opt/yliyunclaw-offline/yliyunclaw-runtime-images.tar` with `.sha256`. |
| Meta Y visible brand alignment | done | Replace the previous visible product naming with Meta Y across UI, desktop shell, skills, templates, and deployment docs while preserving legacy technical identifiers that affect upgrades. | UI title/login/sidebar/about/welcome/invite strings, i18n product descriptions, desktop window/menu/product name, install guidance skills, skill-template placeholders, SearXNG display name, and deployment docs now show Meta Y. Desktop config now uses `.metay-desktop` and `METAY_*` env vars first, with migration/fallback from legacy config/env names. Verification: `vue-tsc --noEmit`, `node --check mateclaw-desktop/electron/main.cjs`, and `node --check mateclaw-desktop/electron/desktopConfig.cjs` passed. |
| Console onboarding UI polish | done | Remove login-page default credential exposure, add a client download entry, make the workspace switcher self-explanatory, and improve the Knowledge Base list visual affordance. | Login no longer renders or carries the default admin/password hint; the sidebar footer now exposes a configurable desktop-client download CTA (`VITE_DESKTOP_DOWNLOAD_URL`, fallback `/downloads/MetaY-Desktop.exe`) with advanced-feature copy; the workspace switcher shows a `Workspace`/`工作空间` label above the active workspace name; the Wiki KB list now uses a book-style icon tile, card border, active accent, and clearer spacing. Verification: `vue-tsc --noEmit` and `git diff --check` passed. |
| Console/App release polish and test deployment | done | Move the workspace label outside the switch button, hide web-only download CTA inside the desktop app, restrict system diagnostics to global admins, fix desktop token persistence, and publish a verified build to the 192.168.0.50 test environment. | UI now treats the workspace label as an external sidebar label; client download CTA is hidden when `window.mateclawDesktop` is present; diagnostics entry is admin-only; Electron local server now prefers stable port `17737` (`METAY_DESKTOP_PORT` override, legacy env fallbacks) so app localStorage survives restarts. Built backend jar, frontend static bundle, and desktop portable package; copied `MetaY-Desktop.exe` into `/downloads/MetaY-Desktop.exe`; packaged runtime assets; deployed to `192.168.0.50` under `/opt/mateclaw-dev/docker`; restarted `yliyunclaw-server`; `/actuator/health` returned `UP`; `http://192.168.0.50:18080/downloads/MetaY-Desktop.exe` returned HTTP 200. Verification: `vue-tsc --noEmit`, backend Maven package with IntelliJ JBR/Maven and `-DskipTests`, desktop `pnpm run dist:portable`, `node --check` for Electron entry files, and `git diff --check` passed. |
| Console permission and token-stat polish | done | Make permission-denied interactions visible for normal users, add a confirmation gate for risky full project access, and improve token usage readability for large values. | Workspace switcher "manage workspaces" now shows a no-permission warning for non-admin users instead of silently navigating into a guarded route; Chat permission mode switching asks for confirmation before enabling full access and renders the active full-access pill/option in red; Token Usage summary and token table columns now display compact K/M/B units while preserving exact values in tooltips. Verification: `vue-tsc --noEmit` and scoped `git diff --check` passed. |
| Conversation runtime model scope and fallback | done | Keep model switching scoped to the current Chat conversation, persist the override on the conversation, and auto-fallback when that stored provider/model later becomes unavailable. | Chat model switching now persists `runtimeProviderId` / `runtimeModelName` on the conversation instead of page-wide local storage, normal send / replay paths always transmit those runtime fields, and backend conversation resolution now auto-heals stale overrides by picking the best currently usable provider/model in system priority order (requested model if still usable → system default → fallback-priority provider chain). The Chat UI warns when a stored conversation model is auto-switched or cleared because the original provider/model is no longer available. |
| Wiki page action regression | done | Fix Wiki page actions that looked unresponsive when adding cross-links, repairing a page, or deleting a page. | Mounted the global `McConfirmHost` so `mcConfirm(...)` callers render an actual confirmation dialog; Wiki page delete now confirms, deletes, refreshes the page list, and shows success/error feedback; cross-link enrichment and page repair now show loading state, submit the backend job, surface job ids or errors, and refresh the current page/page list after the async job has had time to run. Verification: `vue-tsc --noEmit` and scoped `git diff --check` passed. |

## Phase Tracker

| Phase | Status | Goal | Modules | Modified files | Acceptance | Test result | Residual risk |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Phase 0 - Baseline and ledger | done | Establish this ledger and freeze legacy plan inputs. | docs | `docs/agent-harness-implementation.md` | Ledger exists, baseline recorded, legacy items mapped. | Verified by file creation and `git status --short`. | None. |
| Phase 1 - Account types and simplified UX | done | Enforce global `admin/user` and simplify normal user UI. | auth, security, router, layout, chat, agents | `mateclaw-server/src/main/java/vip/mate/config/SecurityConfig.java`; `mateclaw-ui/src/utils/access.ts`; `mateclaw-ui/src/router/index.ts`; `mateclaw-ui/src/views/layout/MainLayout.vue`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/views/Agents.vue` | Admin sees full console; user sees simplified console; protected APIs return 403 when bypassed. | `pnpm exec vue-tsc --noEmit` passed. Backend compile not run: `mvn` is not installed and no Maven wrapper exists in repo. | Workspace owner/admin exceptions for non-global-admin advanced pages need a follow-up policy layer. Backend check is currently global-admin for system-level admin surfaces. |
| Phase 2 - Harness run model | done | Add standard run/step/tool/approval/summary model. | agent, harness, approval, cron, subagent | `mateclaw-server/src/main/java/vip/mate/harness/service/HarnessRunService.java`; `mateclaw-server/src/main/java/vip/mate/agent/AgentGraphBuilder.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/StateGraphReActAgent.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/plan/StateGraphPlanExecuteAgent.java` | ReAct/Plan runs now create unified in-memory harness runs with step, tool, approval, and summary aggregation while preserving current chat/SSE APIs. | IDE diagnostics via `get_errors` reported no errors on modified Java files. Maven compile still not run because `mvn`/wrapper is unavailable in this workspace. | Approval resolution is hydrated from DB on read; full push-based run updates for external approval resolution can be added in a later phase if needed. |
| Phase 3 - Tool metadata and safety policy | done | Attach risk/read-only metadata to all tool sources. | tool, mcp, plugin, skill, guard, auth, workspace | `mateclaw-server/src/main/java/vip/mate/tool/metadata/ToolSourceType.java`; `mateclaw-server/src/main/java/vip/mate/tool/metadata/ToolRuntimeMetadata.java`; `mateclaw-server/src/main/java/vip/mate/tool/metadata/ToolMetadataResolver.java`; `mateclaw-server/src/main/java/vip/mate/tool/guard/model/ToolInvocationContext.java`; `mateclaw-server/src/main/java/vip/mate/tool/guard/model/GuardFinding.java`; `mateclaw-server/src/main/java/vip/mate/tool/guard/engine/ToolGuardEngine.java`; `mateclaw-server/src/main/java/vip/mate/tool/guard/engine/ToolPolicyResolver.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/service/WorkspaceService.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/executor/ToolExecutionExecutor.java`; `mateclaw-server/src/main/java/vip/mate/agent/AgentGraphBuilder.java` | Runtime tool metadata is now unified across built-in/MCP/plugin/skill callbacks, guard decisions include account type and workspace policy mode, and audit findings retain metadata context. | IDE diagnostics via `get_errors` reported no errors on modified Java files. `git status` was checked through changed-files inspection. Maven compile still not run because `mvn`/wrapper is unavailable in this workspace. | MCP/plugin callbacks still rely on heuristic classification when richer provider-side metadata is absent, so future Phase 4 workspace-policy overrides should remain conservative by default. |
| Phase 4 - Workspace policy and Coding Agent | in_progress | Add Codex-style coding workflow and workspace policy. | workspace, chat, file tools, git surface, tool guard, ui, templates | Representative landings: `WorkspacePolicy` / `WorkspaceService`; `ConversationService`; `ToolPolicyResolver` / `ToolExecutionExecutor`; `AgentGraphBuilder`; `TemplateService` / `TemplateController`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/components/chat/MessageBubble.vue`; `mateclaw-ui/src/components/chat/ProjectChangesPanel.vue`; `mateclaw-ui/src/components/workspace/WorkspaceSwitcher.vue`; `mateclaw-ui/src/views/Security/Workspaces/index.vue`; related API/types/locale/template JSON files. | Workspace policy is persisted and merged into runtime guard decisions; Coding-oriented starter templates create usable agents; Chat and Agent Studio now expose workspace-aware template entry, project/change surfaces, approvals/timeline context, coding-preferred runtime defaults, a conversation-scoped runtime model override with automatic unavailable-model fallback/prompting, and a project locator that resolves the nearest manifest/git project root plus current git-scoped changed files. Approval banners now also surface workspace-boundary / policy causes and recovery wording directly from guard findings, while Workspace Policy screens clarify how workspace roots, allowed/denied paths, sandbox, and permission mode interact. Remaining work is to keep shrinking the generic runtime toggle in favor of profile/capability/template-driven entry. | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed after the latest template/runtime UX updates. `get_errors` reported no new Java issues in the touched template/controller files; existing frontend alias-resolution diagnostics remain baseline noise. Backend compile is still blocked by missing `mvn` / Maven wrapper. | Specialized Coding Agent entry expansion is temporarily deferred while the built-in teacher flow is simplified first; remaining work still includes an end-to-end real coding-task acceptance run, stronger command/test verification, and tighter integration with Phase 5 cache/router work to reduce repeated project scans. |
| Phase 5 - Project cache and context router | in_progress | Reduce repeated scans and route context by source. | memory, wiki, workspace, agent context | `mateclaw-server/src/main/java/vip/mate/workspace/core/model/ProjectInsightSummary.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/model/ContextRouterSummary.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/service/WorkspaceService.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/controller/WorkspaceController.java`; `mateclaw-server/src/main/java/vip/mate/agent/context/ContextRouterService.java`; `mateclaw-server/src/main/java/vip/mate/memory/lifecycle/MemoryLifecycleMediator.java`; `mateclaw-ui/src/api/index.ts`; `mateclaw-ui/src/types/index.ts`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/components/chat/ProjectChangesPanel.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | 首版项目理解缓存已可按 workspace + project 路径返回技术栈、关键文件、目录提示与常用命令，并在 Chat Project 面板展示，避免同一路径重复全量扫描。新增统一 Context Router 摘要接口后，Chat Project 面板现在还能按当前 workspace / agent / conversation 展示工作区记忆文件、模板知识键、Wiki 路由命中与近期会话来源，形成后续真正 prompt/router 编排前的可观测层；同时该摘要的精简版本已在每轮对话前通过 lifecycle mediator 注入运行时消息上下文，开始实际影响 prompt 路由。最新一轮又将注入块升级为 query-aware + budget-aware：会按当前用户问题优先挑选项目命令/关键文件、启用中的工作区记忆文件、命中的历史会话片段与知识路由提示，并在固定字符预算内裁剪，减少无差别摘要堆叠。现在项目理解缓存已按 workspace + located project 共享结构摘要，并用根目录/关键标记文件指纹做轻量失效；当前工作目录与 git 变更摘要则在请求时重新水合，减少重复目录扫描同时保持项目变更视图新鲜。 | `mateclaw-ui/node_modules/.bin/vue-tsc.CMD --noEmit` passed. Java diagnostics via `get_errors` reported no errors on `ContextRouterService.java` and `MemoryLifecycleMediator.java`. Backend compile still pending because `mvn`/wrapper is unavailable in this workspace. | 当前项目缓存已具备轻量指纹失效，但还不是深度文件系统 watcher；Context Router 已具备 query-aware 与预算裁剪，但仍是字符级近似预算，尚未接入真实 token 计数、Wiki 命中片段压缩与更细粒度的多源检索编排。 |
| Phase 6 - Agent profiles and domain packs | in_progress | Add reusable profiles and capability packs. | agent, skill, datasource, templates | Core landings: `docs/agent-harness-implementation.md`; `TemplateDTO`; built-in template JSON seeds for Coding Agent and Teacher Exam Assistant; shared frontend template types. Detailed runtime binding and KB/UI polish follow-ups are tracked in the Phase 6 execution notes below. | Built-in coding and education templates now carry structured profile/capability/policy/tool/knowledge metadata, starter KB/page seeds, quality gates, mock acceptance declarations, and template-driven interaction hints/starter prompts. The remaining gap is full runtime materialization through thin chat/runtime surfaces, stronger context-router consumers, and end-to-end mock runs rather than dedicated heavy forms. | Template JSON files re-validated with PowerShell `ConvertFrom-Json`; `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed. Backend compile remains constrained by missing `mvn` / Maven wrapper. | Template metadata is now rich and durable, including first-class DTO/frontend typing for template-driven interaction hints, but the product still needs deeper runtime consumers so profile, capability, knowledge, and policy declarations behave as execution inputs rather than mostly prompt/DTO metadata, especially for teacher-side source-grounding and rubric enforcement. |
| Phase 7 - Channel/Cron/plugin convergence | deferred | Normalize inbound events and plugin capability declarations. | channel, cron, plugin, skill market | pending | Channel/Cron runs are auditable and plugin capabilities are diagnosable. | Deferred by current delivery priority: core business path and two built-in Agent application cases land first. | Broad integration surface. |

## Phase 6 Execution Notes

| Step | Status | Scope | Modified files | Verification | Residual risk |
| --- | --- | --- | --- | --- | --- |
| Template requirement intake | done | Fold Codex-style coding agent and teacher exam assistant requirements into the canonical ledger and register both as built-in JSON seeds. | `docs/agent-harness-implementation.md`; `mateclaw-server/src/main/java/vip/mate/agent/model/TemplateDTO.java`; `mateclaw-server/src/main/resources/templates/coding-agent.json`; `mateclaw-server/src/main/resources/templates/teacher-exam-assistant.json`; `mateclaw-ui/src/types/index.ts` | New JSON templates parsed successfully with PowerShell `ConvertFrom-Json`; `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed. | Template metadata was exposed on DTO responses, but agent instances still needed a durable binding to their source template. |
| Teacher lightweight chat guidance | done | Keep the teacher chat window concise by rendering a template-driven guidance card and starter prompts in Chat instead of a dedicated task-builder form, and keep sample-task evidence collapsed behind explicit disclosure by default. | `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-server/src/main/resources/templates/teacher-exam-assistant.json`; `docs/agent-harness-implementation.md` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; the teacher template re-parsed successfully with `ConvertFrom-Json`; current Chat now reads `interactionHints` / `starterPrompts` directly from template metadata, and the sample-task card stays as a one-line summary until the user expands details. | No large dedicated teacher form is planned. Remaining work is to strengthen runtime grounding and scoring, not to expand visible teacher-side configuration UI. |
| Template binding persistence | done | Persist template id/version/category/domain, AgentProfile id, CapabilityPack id and a template manifest snapshot on Agent instances created from built-in templates. | `mateclaw-server/src/main/java/vip/mate/agent/model/AgentEntity.java`; `mateclaw-server/src/main/java/vip/mate/agent/service/TemplateService.java`; `mateclaw-server/src/main/resources/db/migration/h2/V98__agent_template_binding.sql`; `mateclaw-server/src/main/resources/db/migration/mysql/V98__agent_template_binding.sql`; `mateclaw-ui/src/types/index.ts` | Template JSON profile/capability ids parsed successfully; `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; migration files reviewed for H2/MySQL idempotent add-column/index pattern. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | The manifest snapshot is durable, but Phase 6 still needs runtime consumers that enforce capability/knowledge/policy bindings from the snapshot. |
| Template runtime guidance | done | Consume the stored template manifest at Agent build time and inject concise runtime guidance for profile, capability pack, policy, knowledge bindings, declared tools and quality gates. | `mateclaw-server/src/main/java/vip/mate/agent/AgentGraphBuilder.java`; `docs/agent-harness-implementation.md` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; both built-in template JSON files parsed successfully and exposed expected tool/gate counts; static Java review confirmed Java 21 compatibility. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | This is prompt-level consumption only. Hard enforcement still belongs in Tool Guard, Workspace Policy and Context Router consumers. |
| Template policy guard merge | done | Pass template manifest policy into tool execution and merge it with workspace policy so template sandbox/network/paths/risk overrides can only tighten Guard decisions. | `mateclaw-server/src/main/java/vip/mate/agent/AgentGraphBuilder.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/executor/ToolExecutionExecutor.java`; `docs/agent-harness-implementation.md` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; both built-in template policies parsed successfully; static constructor review confirmed legacy executor overloads still delegate with null template metadata. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | This covers Tool Guard policy consumption. Context Router still needs to enforce knowledge/session-source routing from template bindings. |
| Template default knowledge seeds | done | Built-in templates may seed default KB/pages as sample data, but seed identity no longer depends on `externalKey`; default KBs are matched by workspace + name and pages by slug. Runtime wiki context follows explicit Agent KB bindings. | `mateclaw-server/src/main/java/vip/mate/agent/service/TemplateService.java`; `mateclaw-server/src/main/java/vip/mate/wiki/service/WikiKnowledgeBaseService.java`; `mateclaw-server/src/main/resources/templates/coding-agent.json`; `mateclaw-server/src/main/resources/templates/teacher-exam-assistant.json`; `docs/agent-harness-implementation.md` | Template JSON parsed successfully; `externalKey` was removed from the two built-in template seed declarations. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | Existing DB column and legacy API field remain for backward compatibility, but the current built-in flow no longer requires users to understand or configure it. |
| Agent knowledge binding UI | done | Knowledge bases are selected directly in Agent create/edit as optional session context. The Wiki page no longer exposes template binding key configuration to normal business users. | `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/views/Wiki/index.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts`; `mateclaw-ui/src/types/index.ts`; `mateclaw-server/src/main/java/vip/mate/wiki/service/WikiContextService.java`; `mateclaw-server/src/main/java/vip/mate/agent/context/ContextRouterService.java` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed. An Agent can bind/unbind KBs; no binding means no automatic wiki context injection. | A future normalized join table can replace the Agent JSON id list if KB permissions and batch operations become more complex. |
| Optional Agent knowledge binding | done | Make knowledge bases optional Agent-level session context. Template default KB seed data remains available, but an Agent only injects wiki context when the user explicitly binds KBs in the Agent configuration. | `mateclaw-server/src/main/java/vip/mate/agent/model/AgentEntity.java`; `mateclaw-server/src/main/java/vip/mate/wiki/service/WikiKnowledgeBaseService.java`; `mateclaw-server/src/main/java/vip/mate/wiki/service/WikiContextService.java`; `mateclaw-server/src/main/java/vip/mate/agent/context/ContextRouterService.java`; `mateclaw-server/src/main/resources/db/migration/h2/V100__agent_knowledge_bindings.sql`; `mateclaw-server/src/main/resources/db/migration/mysql/V100__agent_knowledge_bindings.sql`; `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/types/index.ts`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; built-in template JSON parsed successfully. Template cards no longer show misleading `0/0` knowledge initialization for templates without default KB seeds, and Agent edit/create now supports bind/unbind KBs. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | Existing databases need V100 migration. Knowledge binding is stored as a JSON id list on Agent; a later normalization pass can move this to a join table if many-to-many management becomes more complex. |
| Wiki / KB management polish | done | Fill the remaining KB management gaps and fix the crowded knowledge/page navigation UX: KB creator-or-admin delete, nav-style Wiki sidebar, and multi-select KB picker polish for Agent edit. | `mateclaw-server/src/main/java/vip/mate/wiki/controller/WikiController.java`; `mateclaw-server/src/main/java/vip/mate/wiki/model/WikiKnowledgeBaseEntity.java`; `mateclaw-server/src/main/java/vip/mate/wiki/service/WikiKnowledgeBaseService.java`; `mateclaw-server/src/main/java/vip/mate/agent/controller/TemplateController.java`; `mateclaw-server/src/main/java/vip/mate/agent/service/TemplateService.java`; `mateclaw-server/src/main/resources/db/migration/h2/V101__wiki_kb_creator_user_id.sql`; `mateclaw-server/src/main/resources/db/migration/mysql/V101__wiki_kb_creator_user_id.sql`; `mateclaw-ui/src/views/Wiki/index.vue`; `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/stores/useWikiStore.ts`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | KB deletion is now available from the Wiki list for creators and admins; template-created KBs persist creator identity; Wiki left navigation separates KB list from per-KB page/archive navigation; Agent KB select dropdown no longer collapses selected tags into `+N`, option rows align correctly, and reopening edit shows bound KBs checked in the list. `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` was re-run after the UI polish pass; IDE diagnostics on touched Java files reported no new backend errors. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | Historical KB rows created before V101 may not have creator metadata, so creator-based delete cannot be inferred for those rows; they still require admin cleanup. |
| Auth/session flow hardening | done | Consolidate frontend auth storage and redirect behavior so login persistence, request injection, token renewal, and logout all use one shared session path. | `mateclaw-ui/src/utils/auth.ts`; `mateclaw-ui/src/api/index.ts`; `mateclaw-ui/src/views/Login.vue`; `mateclaw-ui/src/router/index.ts`; `mateclaw-ui/src/views/layout/MainLayout.vue` | Frontend now stores and clears session state through shared helpers, request/stream code reads the same token source, and 401 handling funnels through unified logout/redirect logic. Targeted diagnostics reported no errors in the touched files; `vue-tsc --noEmit` completed after the change. | Still needs a focused follow-up on any remaining post-logout session-fetch noise from secondary consumers. |
| Wiki delete dialog consistency | done | Remove browser-native delete confirms from KB/page deletion flows and reuse the product confirmation modal. | `mateclaw-ui/src/views/Wiki/index.vue`; `mateclaw-ui/src/views/Wiki/components/WikiPageViewer.vue` | Wiki destructive actions now use `mcConfirm(...)`, with failure feedback moved to `ElMessage.error(...)` for consistent in-app UX. Existing Vue alias/default-export diagnostics remain baseline noise rather than regressions from this pass. | Additional destructive flows outside Wiki can be normalized later if needed. |
| Desktop client-only packaging defaults | done | Align desktop runtime defaults and docs with a client-only packaged shell that targets hosted MateClaw by default but still supports local/custom backends. | `mateclaw-desktop/electron/desktopConfig.cjs`; `mateclaw-desktop/electron/main.cjs`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts`; `mateclaw-desktop/README.md`; `README.md` | Dev mode still defaults to `http://127.0.0.1:18088`; packaged mode now defaults to `https://claw-demo.mate.vip`; Node syntax checks passed for the touched Electron files. | The product still ships optional local-backend launch hooks, so release packaging/documentation should continue to emphasize that bundled local runtime is no longer the default distribution model. |
| Workspace invite links | done | Add shareable invite-link generation and acceptance flow on top of existing workspace member roles. | `mateclaw-server/src/main/java/vip/mate/auth/service/AuthService.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/service/WorkspaceService.java`; `mateclaw-server/src/main/java/vip/mate/workspace/core/controller/WorkspaceController.java`; `mateclaw-ui/src/api/index.ts`; `mateclaw-ui/src/router/index.ts`; `mateclaw-ui/src/views/Login.vue`; `mateclaw-ui/src/views/Security/Members/index.vue`; `mateclaw-ui/src/views/WorkspaceInvite.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | Workspace admins can now create signed invite links for `admin/member/viewer`; recipients can preview and accept invites on a dedicated page, while login now preserves the redirect target so invite acceptance resumes after authentication. `pnpm exec vue-tsc --noEmit` completed after the frontend changes; Java diagnostics reported no new issues in the touched backend files. | Invite links are currently stateless signed tokens rather than DB-backed pending invitations, so explicit revoke/list-audit flows remain a possible later enhancement if operators need fine-grained invite lifecycle management. |
| Logout toast suppression | done | Prevent post-logout conversation/session fetches from surfacing misleading load-failure toasts during redirect. | `mateclaw-ui/src/utils/auth.ts`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/views/Sessions.vue` | Shared auth utilities now expose redirect-in-progress state, and Chat / Sessions suppress load-failure messages once logout has already cleared the token and started redirecting. Targeted diagnostics reported no new errors in `auth.ts`; Vue file diagnostics still show the existing alias-resolution baseline noise rather than change-specific regressions. | Other screens that intentionally keep polling after logout are not expected, but the same guard can be reused if similar noise appears elsewhere. |

## P1 Built-in Agent Application Case Checklist

| Case | Status | Required default data | Required runtime binding | UI/UX acceptance | Residual risk |
| --- | --- | --- | --- | --- | --- |
| Coding Agent | in_progress | Built-in Agent template, default Agent profile, capability pack, workspace policy, coding tool declarations, project memory defaults, mock repository acceptance tasks. Default KB seed creates starter coding rules pages; Agent workspace files include `MEMORY.md` and `ACCEPTANCE.md`. | Template snapshot is persisted; runtime guidance and tool policy merge are active; project cache/context summary is partially active; applying the template seeds missing default KB pages without overwriting existing pages; missing default Agent files can be synced without overwriting user edits. | User can create the Agent from Starter templates, enter Chat, use coding runtime, see project/context/run/change panels, inspect template completeness, sync missing default files, and launch the first mock acceptance task into Chat. Chat now writes template/task metadata into HarnessRun metadata, surfaces final-answer preview, and shows a first-pass automatic score derived from plan/write/validation signals plus matched acceptance points. Structured evidence now includes touched-file extraction, focused-edit heuristics, and validation command/result snippets when available. | Auto scoring now captures file scope and validation evidence from tool traces, but it still needs true diff-aware assertions and repo-level checks before it can be treated as a hard acceptance gate. |
| Teacher Exam Assistant | in_progress | Built-in Agent template, default education profile, capability pack, optional Wiki knowledge keys, scoring rubric memory, mock exam-generation acceptance tasks. Default KB seeds now create “初中名著阅读评分参考”, “初中语文题型示例”, plus factual seed libraries for “七年级上册名著事实素材” and “八年级下册名著事实素材”; hard exam rules are embedded in `systemPrompt`, `AGENTS.md`, and `ACCEPTANCE.md`; Agent workspace files include `MEMORY.md` and `ACCEPTANCE.md`. | Template snapshot is persisted; Wiki context is injected only from KBs explicitly selected in Agent configuration; applying the template seeds missing default KB pages without overwriting existing pages; the existing “同步默认文件” repair path now also补齐缺失知识库与种子页 so older sample Agents can recover missing teacher materials without re-creation. The teacher template policy now uses approval-friendly `networkPolicy=restricted`, and runtime policy resolution treats built-in web retrieval as network-sensitive so a missing-materials web fetch can surface an approval prompt instead of being silently hard-blocked by the old template default. | User can create the Agent, optionally bind/unbind knowledge bases in Agent configuration, see Starter-card default knowledge seed status, manage crowded KB inventories through nav-style Wiki sidebar plus creator/admin delete, sync missing default files, launch the first mock acceptance task into Chat, and chat with or without wiki context. Chat now retains template/task metadata on the run and shows a first-pass automatic score for source-grounding / answer-rubric / coverage signals alongside the answer preview, plus extracted source hints such as referenced books / KB mentions, knowledge-tool usage, retrieved page/raw-source traces, and whether the final answer cites retrieved sources. The latest formal-acceptance diagnosis found two concrete blockers from production logs: (1) built-in `wiki_*` / `session_search` / `fact_probe` reads were being misclassified as mutating high-risk tools and blocked by workspace policy, and (2) the default teacher KB seeds lacked enough factual `西游记` / `昆虫记` / `经典常谈` material for the sample tasks. Both have now been addressed in code and template seeds, and the next approval-specific hard block has been narrowed by steering teacher runs toward built-in web search approval instead of shell/browser bypass attempts. The chat composer still favors a lightweight template-driven guidance card plus sample prompts instead of a large visible form, and the sample-task card remains collapsed to a one-line summary by default. | Current checklist focus: run at least one post-fix real end-to-end sample task against the repaired KB/runtime path and then decide whether teacher scoring still needs deeper section-aware rubric checks beyond the existing source / answer / review gates. Lightweight/template-driven UX is now the locked-in direction rather than an open design item. |
| Template health visibility | done | Expose per-workspace template readiness for built-in application cases, including required KB matches, seeded page counts, runtime/profile/capability/tool declarations, memory files, acceptance files, and mock tasks. | Backend `GET /api/v1/templates/health` returns `TemplateHealthDTO`; Agent Studio template cards show knowledge readiness and application-case check counts before applying a template. | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; both template JSON files parsed as UTF-8 and retain expected tool/mock/file counts (`coding`: 10/3/5, `teacher`: 8/3/4). | Health verifies template structure and default KB readiness; materialized Agent evidence is tracked in the next row. |
| Template application evidence | done | Show whether each built-in template has actually been applied in the active workspace and whether the generated sample Agent still has the expected workspace files, memory file, acceptance file and prompt-file configuration. | `TemplateHealthDTO` now includes application evidence; `TemplateService` checks applied Agents by `templateId`; Agent Studio template cards show created-sample status after applying a starter template. | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed; both template JSON files parsed successfully (`coding`: tools=10/tasks=3/files=5/kb=1, `teacher`: tools=8/tasks=3/files=4/kb=2). Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | This verifies sample Agent materialization and file integrity, but it does not yet execute the mock tasks through an LLM/tool run. That remains the next P1 acceptance step. |
| Template default file sync | done | Allow existing Agents created from older template versions to sync missing default workspace files without overwriting user-edited files. | `mateclaw-server/src/main/java/vip/mate/agent/service/TemplateService.java`; `mateclaw-server/src/main/java/vip/mate/agent/controller/TemplateController.java`; `mateclaw-ui/src/api/index.ts`; `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed. The template card shows "同步默认文件" when applied Agent files are incomplete; sync only creates missing files and merges enabled prompt files. Backend compile remains constrained by missing `mvn` and missing Maven wrapper. | Existing user-edited files are intentionally not overwritten; future UI can show exact missing filenames before syncing. |
| Mock acceptance task launcher | done | Make template mock acceptance tasks user-runnable from Agent Studio by opening Chat with the applied Agent and prefilled sample task. The route now carries template/task metadata so Chat can display the active sample task, acceptance checks, and live HarnessRun evidence counts. | `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | Template cards now expose every declared mock acceptance task, while the original quick “Run sample task” action still launches the first task. `mateclaw-ui/.node_modules/.bin/vue-tsc.cmd --noEmit` passed. Built-in templates parse with mock counts (`coding`: 3, `teacher`: 3). | The run is now identifiable and observable from Chat; automatic scoring exists, but repo-level hard assertions and real LLM acceptance runs remain the next strengthening step. |
| Mock acceptance evidence auto-link | done | Carry template sample-task metadata through Web chat origin into HarnessRun metadata, and expose final-answer preview plus rough matched-acceptance counts in Chat so built-in Agents have an end-to-end observable sample-task loop. | `mateclaw-server/src/main/java/vip/mate/agent/context/ChatOrigin.java`; `mateclaw-server/src/main/java/vip/mate/channel/ChannelChatOriginFactory.java`; `mateclaw-server/src/main/java/vip/mate/channel/web/ChatController.java`; `mateclaw-server/src/main/java/vip/mate/harness/model/HarnessExecutionSummary.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/StateGraphReActAgent.java`; `mateclaw-server/src/main/java/vip/mate/agent/graph/plan/StateGraphPlanExecuteAgent.java`; `mateclaw-ui/src/composables/chat/useChat.ts`; `mateclaw-ui/src/views/Agents.vue`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/types/index.ts`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | Java diagnostics on touched backend files reported no new errors. Frontend types were extended for summary preview / mock task metadata; workspace diagnostics remain limited by existing alias-resolution baseline noise, but the project already passes `vue-tsc --noEmit` in the workspace terminal. | Matching is currently heuristic and based on final-answer preview text; true scoring still needs structured rubric evaluation, coding diff/test evidence checks, and teacher-template source-grounding validation. |
| Mock acceptance auto scoring | done | Derive a first-pass structured score from HarnessRun metadata, tool traces, run status, and final-answer preview so sample tasks report passed/partial/failed plus supporting signals directly in Chat. Follow-up strengthening now extracts touched files, focused-edit heuristics, validation commands/results, teacher-side source hints / knowledge-tool usage, patch-aware diff stats from write-tool traces, and retrieved/cited source traces from knowledge-tool outputs into the assessment evidence payload. Coding tasks now also enforce simple pass gates: mutation-required runs need material diff evidence, validation-required runs need a successful validation result, and read-only tasks cannot mutate files. Teacher tasks now also require source grounding, cite retrieved sources when retrieval happened, and include answer / scoring-rubric evidence before a full pass is allowed. Validation evidence additionally parses explicit exit codes when tool outputs provide them, reducing false positives from plain-text matching, and expected-item matching now distinguishes combined diff+validation requirements plus stronger teacher-source requirements from the structured signals instead of relying only on preview text. | `mateclaw-server/src/main/java/vip/mate/harness/service/HarnessRunService.java`; `mateclaw-ui/src/views/ChatConsole.vue`; `mateclaw-ui/src/i18n/locales/zh-CN.ts`; `mateclaw-ui/src/i18n/locales/en-US.ts` | Chat now renders automatic score, confidence, matched/missing acceptance items, scoring signals, dedicated structured validation / diff / source sections, and explicit acceptance blockers for built-in sample tasks as the run progresses. Harness-side evidence now also preserves patch file counts / added-removed lines together with file / validation / source snippets for better operator review. Teacher scoring additionally records retrieved source titles/files, whether the answer cites them, and blocks full pass when source grounding or answer / rubric evidence is missing. | Current scoring is stronger for coding runs because missing diff / validation success now blocks a full pass and surfaces concrete blockers, while teacher runs now also block full pass on missing source grounding / rubric evidence; it still remains heuristic, so repo-level checks and deeper rubric-grounding are the next steps before gate-level acceptance. |

## Teacher Agent 出题闭环专项

| Task | Status | Scope | Acceptance |
| --- | --- | --- | --- |
| 流程门控 | done | 仅在 Teacher Agent + 出题类任务 + plan 模式下强制“先方案、后确认、再出题”；普通问答、材料总结、使用说明、非 plan 一般对话不强制确认。 | `StateGraphPlanExecuteAgent` 已加入 Teacher workflow guard：首轮出题只返回命题方案并等待确认；等待确认期间非确认回复只更新方案；明确确认后才进入正式出题。2026-05-18 复查后补充“阅读题/选择题/填空题/简答题/探究题”等意图词，降低 plan 模式漏判。 |
| 结构化结果展示 | done | Teacher 正式出题结果按“试题、参考答案、采分点、来源依据”作为客户主交付区块展示；命题说明/出题说明和命题质量审核进入内部折叠区，需要时展开查看。 | `MessageBubble.vue` 已识别标准分块；试题默认展开，答案/采分点/来源折叠，命题说明/质量审核不进入主展示；每块可复制/保存；可导出仅试题版和完整版 Markdown/Word。2026-05-18 复查后分块解析扩展到 JSON、一级/多级 Markdown 标题、加粗标题、`试题内容`/`评分标准` 等常见标题，避免模型标题轻微变化时退回整段展示。2026-05-26 复查后，完整版导出排除命题说明和质量审核。 |
| 资料目录与知识库路径 | done | Teacher 默认不盲目 shell 读取教学目录；优先复用工作区 ProjectInsight 的压缩资料索引并按需读取，必要时再做轻量筛选，超出工作区或受限时走审批或替代路径。 | Teacher plan 模式下检测到本地目录/教学资料请求时，先以用户指定目录为上下文边界；工作区首次进入时由 `ProjectInsightSummary.materialIndexHints` 生成压缩资料索引，并通过 `ContextRouterService` 注入后续会话上下文；后续同一工作区默认复用缓存索引，不再每轮重复 `list_directory` / `index_directory_materials`。只有用户明确刷新/同步、缓存缺失或需要具体子目录/文件列表时才重新扫描；工具层也为 `list_directory` / `index_directory_materials` 增加 30 分钟同参数缓存与 `refresh` 参数兜底，避免模型误调用时重复 walk 文件系统；受限时发起审批或引导改用知识库/会话材料，不进入 shell 猜文件名试错。 |
| 导出 v2 | done | Teacher 结果必须支持导出 Word；未指定路径时默认落到当前项目绑定目录下的 `output/` 文件夹，同时保留浏览器/客户端下载能力。 | 当前支持 Markdown 分块保存、仅试题版/完整版 Word(docx) 导出；后端默认把 docx 写入当前会话项目目录下的 `output/`（自动创建），并返回临时下载链接，所以 Web 与桌面客户端都可直接触发下载。 |
| Harness 验收 | done | 将 teacher 闭环行为纳入 mock acceptance / summary scoring。 | Teacher mock acceptance 现在识别“待确认方案”中间态、正式出题结构化分块、来源 grounding、答案/采分点/质量审核 gate；待确认态不会被正式试题 gate 误判为失败。 |

## Teacher Agent 教学出题闭环专项 v2

Status: `in_progress`
Owner priority: P1 Built-in Agent application cases / Phase 6 Agent profiles and domain packs
Ledger sync date: 2026-05-27
Implementation record: `docs/teacher-agent/teacher-agent-v2-implementation-record.md`
Sync rule: this ledger remains the single source of direction; the implementation record tracks per-stage execution details, touched files, tests, and acceptance results, and must be updated together with this section after each stage.

### Teacher Agent v2.5 规则包、教材课标与学段边界

Status: `done`

| Stage | Status | Goal | Modules | Files | Acceptance | Test result | Open issues |
| --- | --- | --- | --- | --- | --- | --- | --- |
| v2.5-1 六类初中语文 RulePack | done | 在名著、文言文、现代文基础上补齐古诗词、基础知识、写作，并为 RulePack 增加学段、学科、资料要求字段。 | teacher, harness, templates, ui | `TeacherRulePack`; `TeacherRulePackService`; `TeacherIntentService`; `TeacherSkillDefinitionService`; `TeacherAcceptanceService`; `StateGraphPlanExecuteAgent`; `Agents.vue`; `types/index.ts`; `teacher-exam-assistant.json`; `teacher-rules/*` | 名著、文言文、现代文、古诗词、基础知识、写作能分别匹配独立规则包；小学、高中和其他学科不混用初中语文规则。 | `pnpm --dir mateclaw-ui exec vue-tsc --noEmit` passed；6 个 `teacher-rules/*.json` 与 `teacher-exam-assistant.json` `ConvertFrom-Json` passed；Teacher service targeted `javac -proc:none` passed；`git diff --check` passed。 | 规则包资源 JSON 先作为文档源，后续可扩展为完整导入源。 |
| v2.5-2 教材课标与知识库接入边界 | done | 明确知识库是教材/课标/稿件依据来源，RulePack 是硬规则来源；缺少最新教材/课标时必须提示补充，不能假称已按最新资料生成。 | teacher, wiki, context-router | `TeacherRulePackService`; `StateGraphPlanExecuteAgent`; `RawMaterialPanel.vue`; `Agents.vue` | 绑定知识库时优先使用资料来源；未绑定但用户要求最新教材/课标时，方案和正式出题都必须提示导入/绑定或补充材料。 | `vue-tsc --noEmit` passed；JSON validation passed。 | 知识库原始材料专用分类字段可在后续迁移中落库；本轮先通过上传标题类型前缀、RulePack sourceRequirements 和 UI 规则配置承接。 |
| v2.5-3 RulePack 调整体验 | done | 复用“工作区覆盖 > 全局覆盖 > 内置”机制，前端规则详情支持可视化调整学段、学科、模块、题型比例、题型规则、硬性规则、资料要求，并保留 JSON 高级编辑。 | ui, teacher api | `Agents.vue`; `TeacherRulePackController`; `TeacherRulePackService`; `types/index.ts` | 管理员可保存工作区/全局覆盖；普通用户只读；新会话使用有效覆盖规则。 | `vue-tsc --noEmit` passed；Teacher service targeted `javac -proc:none` passed。 | 更细的 workspace owner/admin 权限区分沿用后续权限收口。 |
| T2.5-4 生成产物预览下载 | done | HTML 报表、分析文件、导出文件可在聊天和 Project 面板打开或下载；会话中不内联渲染 HTML 源码。Web 端聊天点击 HTML 报表时在新页签打开预览，Project 面板对生成文件同时提供“预览/下载”入口且不引发会话跳转；App 端保留本地打开/定位。 | generated files, chat ui, project panel | `GeneratedFileController`; `MessageBubble.vue`; `ProjectChangesPanel.vue`; `useMarkdownRenderer.ts`; `types/index.ts` | `app/output/report_classification.html` 这类产物可从聊天区文件入口新页预览（HTML）或下载（非 HTML）；右侧 Project 面板支持按 `app/output`、`output`、文件名等路径映射到生成文件，HTML 走 `/inline` 预览，下载走原始文件接口；会话正文不再把生成 HTML 源码作为正文渲染。 | `pnpm --dir mateclaw-ui exec vue-tsc --noEmit` passed；`git diff --check` passed（仅 CRLF warning）。 | Web 端对宿主机任意本地路径的直接下载仍依赖后续文件代理或桌面能力；当前优先支持生成文件缓存链接下载和 HTML 预览新页打开。 |
| T2.5-5 组卷编排 | done | 支持“一套卷子”：主 Agent 拆分模块，各模块生成结构化题块，主 Agent 合并卷面、答案、采分点和来源。 | teacher, plan agent, rulepack router | `TeacherIntentService`; `StateGraphPlanExecuteAgent`; `TeacherRulePackService` | “组卷/一套卷/综合卷”进入 `paper_assembly`；主 Agent 注入六类 RulePack；结果必须是一份完整试卷，不是多个模块散答；暂不做复杂 subagent UI。 | 代码路径复核通过：`paper_assembly` 意图识别、六类 RulePack 注入与组卷约束仍生效；本轮前端类型检查 `vue-tsc --noEmit` passed。 | 后续可加专门组卷 UI、分值配置器和 subagent 运行可视化。 |
| T2.5-6 HTML 报表模板与高级工具曝光 | done | 保持系统 HTML 导出为默认通路，增强默认模板质感；将 3 个 HTML 导出工具产品化为可手动绑定的高级工具，补齐中文名称/描述；不把“纯模型直写 HTML”设为默认路径。 | html export, tool catalog, agent config, docs | `HtmlExportService`; `messages*.properties`; `db/migration/*/V107__register_html_render_tools.sql`; `Tools.vue`; `zh-CN.ts`; `en-US.ts`; `docs/teacher-agent/iusses.md` | 默认生成的 HTML 报表具备更稳定、更现代的系统模板；Agent 配置页可手动绑定 3 个中文化 HTML 工具；后台工具页展示优先显示 `displayName`；默认仍以系统工具导出为主，仅在用户明确要求时才走模型直写 HTML。 | `Push-Location "d:\project\ai\mateclaw-dev\mateclaw-ui"; .\node_modules\.bin\vue-tsc.cmd --noEmit; Pop-Location` passed；`git -C "d:\project\ai\mateclaw-dev" diff --check` passed（仅现有 CRLF 警告）。 | 后续可继续按业务线增加行业模板、企业主题皮肤和固定报表版式，但不影响当前默认稳定通路。 |

### Teacher Agent v2-2 回归优化计划

Status: `in_progress`

| Stage | Status | Goal | Modules | Planned files | Acceptance | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| T2-2-1 模板与实例同步边界 | done | 明确“模板更新不自动覆盖已创建实例”，提供 Teacher 实例一键同步/修复入口；新模板默认生效，旧实例可选择同步首页引导。 | templates, agents, ui | `Agents.vue`; `types/index.ts`; `teacher-exam-assistant.json`; docs | 已创建 Teacher 实例无需删除重建即可同步到“初中语文命题助手”首页副标题和 4 个快捷入口；同步不会静默保存，用户确认后保存才生效。 | 已在 Agent 编辑页“首页”Tab 增加 Teacher 专用“同步 Teacher 模板首页引导”按钮；同步范围限定为当前 Agent 实例的 `homeSubtitle` / `homeQuickStarts`，不覆盖用户自定义 Prompt、知识库、RulePack、Skill。`vue-tsc --noEmit` passed；`git diff --check` passed。 |
| T2-2-2 Skill 与 RulePack 可见性隔离 | done | 修复“所有系统模板都显示 Teacher Skill/名著闭环”的错误；RulePack/Teacher Skill 只在 Teacher Agent 或具备初中语文能力包的实例中显示。 | skill, rulepack, template ui | `Agents.vue`; `types/index.ts` | Code Agent、通用助手和用户普通 Agent 不显示 Teacher Skill 流程；Teacher 实例显示自己的规则包和 Skill；普通用户只读。 | 已修复模板卡片 Teacher Skill 展示条件：`templateTeacherSkills` 对非 Teacher 模板直接返回空，UI 外层也增加 `isTeacherTemplate(tpl)`；Teacher 模板识别补充 `capability.education.junior_chinese_exam` / legacy pack。`pnpm --dir mateclaw-ui exec vue-tsc --noEmit` passed；`git diff --check` passed。 |
| T2-2-3 Teacher 自优化草案独立化 | done | 将自优化草案从模板配置中移到 Teacher 运营/诊断入口，明确草案来源、影响范围和发布流程。 | improvement drafts, admin ui | `TeacherImprovementDraftService`; `TeacherImprovementController`; `Agents.vue`; `TeacherOps.vue` | 草案按 workspace / agent / run 来源生成；未审核不生效；模板页和智能体列表页不默认暴露给普通配置流程。 | 自优化草案已从模板卡片和智能体列表页移出，归入独立 Teacher 运维页；草案仍沿用现有审核/发布接口，不自动改正式规则。`vue-tsc --noEmit` passed；`git diff --check` passed。 |
| T2-2-4 Agent 列表删除区整理 | done | 默认列表不再直接展示已删除 Agent；已删除只通过筛选条件查看。 | agents ui | `Agents.vue` | 默认列表只显示正常 Agent；切换“已删除”筛选才显示删除项；恢复/永久删除操作保持可用。 | `showDeletedSection` 现在只在 `activeFilter === 'deleted'` 时展示；默认“全部”列表不再附带已删除区。`vue-tsc --noEmit` passed；`git diff --check` passed。 |
| T2-2-5 Teacher 引导式槽位补全与连续会话衔接 | done | 提升首用流畅性：先基于内置规则给默认方案，再逐步追问缺失关键项；用户回答“需要/确认/按这个来”时能承接上一轮建议，不再重新报材料不足。 | teacher intent, plan agent, chat home | `TeacherIntentService`; `StateGraphPlanExecuteAgent` | “七年级上册”后 Agent 给出推荐默认方案；用户回答“需要”时按上一轮建议进入确认/生成，而不是要求重新补充全部字段；缺知识库时可用内置规则生成基础题，但不得假称引用最新教材/课标。 | 已扩展确认意图，短回复“需要/好的/按这个/按上面/继续”等在等待确认态会进入正式生成；等待确认后的补充信息会通过历史 `teacher_exam_module` 恢复上一轮模块，避免“七年级上册”这类槽位补充把模块重置为 unknown。TeacherIntentService targeted `javac` passed；`vue-tsc` passed；`git diff --check` passed。 |
| T2-2-6 首页引导与默认快捷入口重写 | done | 根据“初中语文命题助手”定位重写默认首页副标题和 4 个快捷入口，突出快速开始、按教材/名著出题、上传材料命题、组卷。 | chat ui, template seed | `teacher-exam-assistant.json`; agent home config | 新建 Teacher 实例首页能引导用户一步步补足信息；自定义 Agent 未配置时仍用通用默认入口。 | 模板默认副标题改为“初中语文命题助手”；4 个快捷入口改为快速开始槽位补全、按年级册别生成方案、基于材料/知识库出题、组一套综合卷。模板 JSON validation passed；`vue-tsc --noEmit` passed；旧实例同步入口仍由 T2-2-1 承接。 |
| T2-2-7 v2-2 回归验收 | in_progress | 把模板同步、Skill 隔离、槽位补全、删除列表、首页引导加入专项验收。 | harness, docs | `HarnessRunService` / teacher acceptance service; docs | 至少覆盖：旧实例同步、Code Agent 不显示 Teacher Skill、“需要”承接上一轮、默认列表不展示已删除、无 KB 时按内置规则生成但标注资料边界。 | 已完成代码级回归：`vue-tsc --noEmit`、模板 JSON 校验、`git diff --check` 通过；下一步补充/执行 Teacher v2-2 mock acceptance，把专项状态收口为 done。 |
| T2-2-8 Teacher 运维入口与实例规则配置修正 | done | 新增 Teacher 配置页，并在 Teacher 实例编辑页展示 6 类有效 RulePack。 | teacher ops, agents ui, navigation | `TeacherOps.vue`; `Agents.vue`; `router/index.ts`; `MainLayout.vue`; i18n | Teacher 配置页集中管理 RulePack、Teacher Skill、自优化草案；智能体列表页不再显示自优化草案；已创建 Teacher 实例编辑页显示 Teacher 规则 Tab 和 6 类规则包；非 Teacher Agent 不显示该 Tab。 | 已新增 `/teacher-ops` 管理员配置路由；Teacher 配置页复用现有 RulePack/Skill/Improvement API；Teacher 实例编辑页新增 `Teacher 规则` Tab，显示 6 类 RulePack 并可进入详情。后续 T2-2-9 将入口从左侧导航迁移到插件页，避免通用工作空间菜单常驻 Teacher 业务入口。 |
| T2-2-9 Teacher 内置插件化入口修正 | done | 将 Teacher 业务入口从工作空间通用导航迁移到插件页，作为系统内置业务插件管理。 | plugins, teacher ops, agents ui | `Plugins.vue`; `MainLayout.vue`; `Agents.vue`; docs | 左侧工作空间导航不再显示 Teacher 运维；插件页显示“Teacher 教学命题插件”内置插件卡；管理员从插件卡进入配置页；Teacher 实例规则 Tab 文案说明能力来自内置插件。 | 已移除侧边栏 `/teacher-ops` 项，保留受 admin 权限保护的隐藏配置路由；`Plugins.vue` 新增系统内置插件分组和 Teacher 插件卡，提供“配置插件”“绑定 Agent”入口；Teacher 实例规则说明改为插件来源。`vue-tsc --noEmit` passed；`git diff --check` passed。 |
| T2-2-10 Teacher 插件能力包与学段学科扩展模型 | in_progress | 将 Teacher 插件从“初中语文单点入口”升级为“教学命题平台插件 + 能力包”模型，为小学/高中/其他学科扩展预留结构，并继续收口为“系统公共规则插件 → 模板默认绑定 → Agent 实例覆盖”的统一规则链。 | template metadata, plugins, agents ui, agent bindings, runtime routing, rulepack, skill | `teacher-exam-assistant.json`; `TemplateDTO`; `TemplateService`; `AgentPluginBinding`; `AgentBindingService`; `AgentGraphBuilder`; `TeacherIntentService`; `TeacherRulePackService`; `TeacherSkillDefinitionService`; `Plugins.vue`; `Agents.vue`; `types/index.ts`; docs | 运行时规则优先级明确为“Agent 实例规则 > 模板默认规则 > 系统插件默认规则”，且与 workspace 无关；插件页支持“配置规则/Agent 绑定”二级页返回；模板页支持插件绑定/解绑；Agent 实例支持绑定/解绑/调整 Teacher 规则插件且不回写模板或公共插件。 | T2-2-10a~e 已全部完成：a) 统一命名文案；b) 二级页面与绑定列表；c) 模板卡片插件绑定展示与 pluginBindings 优先解析；d) Agent 编辑页插件绑定/解绑管理与持久化；e) 后端运行时去 workspace 化。`vue-tsc --noEmit` 和 Java lint 均通过。 |

#### T2-2-10 第二阶段确认方案（2026-05-28）

- 规则插件升级为系统级公共插件，建议统一命名为“教学出题规则插件”；当前能力包页面命名为“初中语文出题规则”。
- 插件作为公共规则中心，默认与 workspace、Agent 无强耦合；workspace 仅保留资源边界和权限语义，不再参与 Teacher RulePack / Skill 的生效优先级。
- 模板不再作为 RulePack/Skill 的事实来源，只作为“默认绑定初始化器”：负责减少实例初始化操作，声明默认插件、能力包、默认 RulePack/Skill 选择以及首页引导等。
- Agent 实例是最终运行实体；实例可绑定/解绑/调整 Teacher 规则插件，并允许实例级 override，但不影响模板和系统插件公共配置。
- 多插件策略采用“允许多插件，但同业务域仅允许一个主规则插件生效”；教育出题域当前只允许一个主规则插件处于 enabled + primary 状态。
- 实际会话规则优先级锁定为：`Agent实例规则 > 模板默认规则 > 系统插件默认规则 > 内置兜底`。

#### T2-2-10 第二阶段任务清单

| Task | Status | Scope | Acceptance |
| --- | --- | --- | --- |
| T2-2-10a 系统级公共插件重命名与能力包入口收口 | pending | plugins, navigation, docs | 插件页主卡片文案、二级页标题、入口描述统一为“教学出题规则插件 / 初中语文出题规则”；为后续小学/高中/其他学科能力包扩展保留列表结构。 |
| T2-2-10b 插件页二级页面与 Agent 绑定列表 | pending | plugins ui, router, api | “配置规则”进入规则配置二级页并支持返回插件列表；“Agent 绑定”进入绑定实例列表二级页，展示工作区、Agent 名称、能力包、更新时间，并支持返回。 |
| T2-2-10c 模板默认绑定统一改为插件引用 | pending | templates, template ui, dto, api | 模板管理页支持绑定/解绑规则插件，模板默认配置统一从系统插件能力包引用，模板不再承载独立规则事实源。 |
| T2-2-10d Agent 实例级插件配置与 override 模型 | pending | agent bindings, agents ui, runtime model | Agent 实例支持绑定/解绑/调整 Teacher 规则插件，支持实例级规则 override；实例修改不影响模板与公共插件；同业务域只允许一个主规则插件。 |
| T2-2-10e 运行时规则优先级去 workspace 化 | pending | runtime routing, rulepack, skill | Teacher 会话实际生效规则优先级改为“实例 > 模板 > 系统插件默认”，移除 workspace 在 Teacher RulePack / Skill 解析中的优先级语义。 |

### Requirement Sources

| Source | Scope | Status |
| --- | --- | --- |
| `docs/teacher-agent/iusses.md` | Teacher 内测核心问题索引，覆盖名著范围、上下文串扰、答案采分点、材料题、题型比例、UI 展示等问题。 | synced |
| `docs/teacher-agent/名著题内测.docx` | 名著出题 15 个详细问题与验收风险。 | synced |
| `docs/teacher-agent/初中名著出题要求.docx` | 名著阅读题型、范围、分值、材料和题干规则。 | synced |
| `docs/teacher-agent/初中文言文出题要求.docx` | 文言文来源、题型、限制、考点规则。 | synced |
| `docs/teacher-agent/初中现代文出题要求.docx` | 现代文阅读来源、题型、范围、限制规则。 | synced |

### Current Code Entrypoints

| Area | Current entrypoint | Current state | v2 action |
| --- | --- | --- | --- |
| Built-in Teacher template | `mateclaw-server/src/main/resources/templates/teacher-exam-assistant.json` | 已声明 Teacher 模板、默认规则、知识库 seed、工具、mock tasks、Word 输出格式。 | 拆出可配置规则包，补齐名著/文言文/现代文细化规则和 UI 可调字段，避免只靠 system prompt。 |
| Plan workflow gate | `mateclaw-server/src/main/java/vip/mate/agent/graph/plan/StateGraphPlanExecuteAgent.java` | 已有 Teacher + plan 模式两阶段短路：待确认方案、确认后正式出题。 | 增强任务类型识别、确认态持久化、上下文隔离和“你是谁”等 Agent 身份回答注入。 |
| Structured result scoring | `mateclaw-server/src/main/java/vip/mate/harness/service/HarnessRunService.java` | 已有 Teacher 分块、来源、答案、采分点、质量审核 heuristic gate。 | 升级为规则包驱动评分，加入名著 15 点通用问题、题型比例、分值、材料题、主观题答案硬门控。 |
| Export API | `mateclaw-server/src/main/java/vip/mate/workspace/conversation/controller/ConversationController.java` | 已提供 `/teacher-export` Word 导出接口。 | 复核导出内容来自结构化分块，不从长文本临时猜测；保留仅试题版/完整版。 |
| Frontend result renderer | `mateclaw-ui/src/components/chat/MessageBubble.vue` | 已解析 `questions/answers/scoringRubric/qualityReview/sources`，支持分块复制、Markdown 下载、Word 导出。 | 固化“试题主展示，答案/采分点/来源折叠；命题说明/质量审核默认不作为主结果”的 v2 UI。 |
| Approval input bar | `mateclaw-ui/src/components/chat/ChatInput.vue` | 已有待审批条、一次/会话/项目范围选择、允许/拒绝。 | 参考 Codex 样式增强审批卡：显示命令/工具、工作区、风险、允许范围菜单和自动批准配置入口。 |
| Approval restore and command handling | `mateclaw-ui/src/views/ChatConsole.vue` | 已恢复 pending approval，并处理 `/approve` / `/deny`。 | 将审批交互和 Teacher 资料读取替代路径联动，避免每轮盲目遍历目录。 |
| Teacher chat guide | `mateclaw-ui/src/views/ChatConsole.vue` | 已有 Teacher guide card 和 starter prompts。 | 加入当前 Agent 身份、规则包、绑定知识库/材料状态，让“你是谁”按 Agent 上下文回答。 |

### RulePack / Skill / Self-Improve Boundary

Teacher v2 adopts a controlled self-improvement model inspired by Hermes-style learning loops, but it does not allow the agent to directly mutate production rules.

| Layer | Responsibility | Teacher v2 rule |
| --- | --- | --- |
| RulePack | Hard business rules: module scope, question mix, score ranges, material requirements, required answer/rubric fields, forbidden patterns, acceptance checks. | 名著、文言文、现代文等类型规则必须以 RulePack 为主配置，prompt 只引用和解释规则。 |
| Agent prompt | Role, interaction style, two-phase plan gate, and instructions for selecting/applying RulePacks. | Prompt 不作为唯一规则源，避免规则散落和不可验收。 |
| Knowledge/Wiki | Teaching materials, source texts, examples, curriculum documents, previous papers. | 只承载出题依据，不承载系统级硬规则。 |
| Memory | User/workspace preferences such as grade, textbook version, default difficulty, common output style. | 只承载个性化偏好，不覆盖管理员发布的 RulePack。 |
| Teacher Skill | Executable workflow unit: generate, review, revise, export, or diagnose a paper with selected RulePack and materials. | Skill 负责“怎么执行”，RulePack 负责“按什么规则执行”。 |
| Self-Improve Loop | Summarize Harness failures, extract reusable lessons, propose RulePack/Skill/acceptance changes. | 只能生成草案和建议，必须由 admin/workspace owner 审核发布后才影响新会话。 |

### Priority Implementation Checklist

| Priority | Task | Status | Concrete landing | Acceptance |
| --- | --- | --- | --- | --- |
| P0 | v2 ledger and acceptance matrix | done | 将本专项写入唯一台账，冻结 v1 已完成项，明确 v2 以规则包、状态机和 UI 验收为主。 | 本节成为 Teacher v2 后续推进源；专项实施记录已落到 `docs/teacher-agent/teacher-agent-v2-implementation-record.md`，后续每阶段同步更新总台账和实施记录。 |
| P0 | Teacher turn context and identity grounding | done | 增加 Teacher 会话上下文对象：agent/template/profile、业务模块、题型、知识库绑定、临时材料、当前 plan 状态。普通问答和“你是谁”优先读取当前 Agent 身份与能力，而不是通用模型身份。 | 新增 `TeacherTurnContext` / `TeacherIntentService`，并接入 `StateGraphPlanExecuteAgent`；Teacher + plan 模式下“你是谁/你能做什么/如何使用”走 direct answer，不再进入命题方案待确认；出题意图、确认意图、本地目录范围识别已统一收口。验证：Teacher intent smoke test passed；相关 Java 文件用本地 JDK + `.m2` classpath 联合编译通过。 |
| P0 | Plan confirmation state hardening | done | 将“待确认/修改方案/正式生成”从文本 marker 升级为消息 metadata 或 run metadata；继续保留 marker 作为兼容 fallback。 | `teacher_workflow` SSE 事件现在写入持久化 message metadata；待确认判断优先读取最近消息实体 metadata，文本 marker 仅兼容老消息。验证：`StateGraphPlanExecuteAgent`、`ChatController` 和 Teacher context 服务用本地 JDK + `.m2` classpath 联合编译通过。 |
| P0 | Structured Teacher result v2 contract | done | 定义 `TeacherExamResultV2`：`paper/questions/answers/scoringRubric/sources/internalReview/exportOptions`；命题说明和质量审核默认进入折叠/内部审核区。 | 新增 `TeacherExamResultV2` 后端契约；正式生成 prompt 优先要求 `teacher_exam_result_v2` JSON，Markdown 标题分块作为 fallback；前端 `MessageBubble.vue` 可将结构化对象数组渲染成试题、答案、采分点、来源和质量审核分块。验证：相关 Java 文件 targeted compile passed；`vue-tsc --noEmit` passed；`git diff --check` passed。 |
| P1 | 名著规则包 v2 | done | 把名著 15 点问题转成规则：教材范围、题型比例、分值、材料题必须给材料、主观题必须有答案采分点、微写作仅作附加、填空/表格控制书写量、辩论题答案不可省略、批注题方向明确、避免超纲术语。 | 新增 `teacher-rules/classic-reading-v2.json`、`TeacherRulePack`、`QuestionTypeRule`、`TeacherRulePackService`；Teacher 模板声明默认规则包；正式出题 prompt 注入名著规则摘要；Harness 增加题型比例、材料题、开放题和规则包信号。验证：规则包 JSON 和模板 JSON 均可解析；相关 Java 文件 targeted compile passed；`vue-tsc --noEmit` passed。 |
| P1 | RulePack + Teacher Skill foundation | done | 后端增加规则包/题型配置模型；前端在 Agent 配置中支持查看和调整 Teacher 规则；新增 Teacher Skill 边界，命题、审核、导出和复盘以 Skill 形式承载执行流程。当前已新增 Teacher RulePack API、Teacher Skill API、Teacher Improvement Draft API，并在智能体模板选择页展示内置 Teacher 规则包摘要/完整详情，以及模板绑定的命题闭环和质量审核 Skill；管理员可通过可视化表单或 JSON 保存工作区级 RulePack 覆盖，也可保存全局覆盖和调整默认 Teacher Skill 绑定，三者都持久化到系统设置，普通用户只读。正式出题 prompt 会按“工作区覆盖 > 全局覆盖 > 内置规则包”的优先级注入有效 RulePack，并注入当前激活 Skill 流程。自优化草案可从 Harness run 生成，默认 pending，不会自动发布；管理员接受并选择发布后才写入工作区或全局 RulePack 覆盖。 | 管理员可调整题型比例、默认分值、允许题型、范围约束；普通用户只使用管理员发布配置。Teacher Skill 能声明适用 RulePack、输入/输出和验收目标。Teacher 模板能看到规则包名称、版本、题型规则、硬规则、验收项和默认执行 Skill；管理员能保存/清除工作区 RulePack 覆盖、全局 RulePack 覆盖和 Skill 绑定；自优化草案可生成、接受、拒绝，且未发布前不影响新会话。验证：`vue-tsc --noEmit` passed；Teacher RulePack/Skill/Improvement/Plan Agent 相关 Java 目标类用本地 JBR `javac` + `.m2` classpath 编译通过。 |
| P1 | Teacher context routing without repeated scans | done | Teacher 优先使用已绑定 KB、会话附件摘要、工作区资料索引缓存；只有用户主动刷新/导入/指定新目录时才触发目录扫描。当前已补强 `list_directory` / `index_directory_materials` 的 cache policy、cache hit、next action；Teacher plan/confirmed prompt 明确要求复用 `<context-router>` 的缓存索引，工具返回 `stopRepeatingTraversal` 后停止重扫同一大目录；Harness summary 增加目录遍历、资料索引和缓存命中信号。 | 连续对话不应每轮调用 `List Directory` 遍历本地文件；目录工具命中缓存后模型应读取候选文件或提示知识库/审批替代路径；资料缺失时给出导入知识库/审批/替代路径提示。验证：相关 Java 文件 targeted compile passed；`git diff --check` passed。 |
| P2 | 文言文规则包 | done | 根据 `初中文言文出题要求.docx` 建立来源、题型、考点、分值、禁用项和材料呈现规则。已新增 `teacher.rulepack.classical_chinese.v1`，覆盖文言字词、句子翻译、断句停顿、内容理解、主旨情感、对比迁移；运行时可按文言文意图选择该规则包，待确认方案会写入模块标记，确认轮可恢复规则包。 | 文言文出题不套用名著规则；题目、答案、采分点和材料来源符合专项规则。验证：新增 JSON 逐个 `ConvertFrom-Json` 通过；Teacher RulePack/Intent/Plan Agent 相关 Java 目标类用本地 JBR `javac` + `.m2` classpath 编译通过。 |
| P2 | 现代文规则包 | done | 根据 `初中现代文出题要求.docx` 建立文本来源、题型、阅读材料、答案采分、禁用项规则。已新增 `teacher.rulepack.modern_reading.v1`，覆盖信息提取、内容概括、语言赏析、结构作用、主旨情感、拓展任务；规则要求先识别文体并确保答案源于文本，不默认复用课内课文。 | 现代文出题不套用名著规则；能围绕材料出题并给出可判分答案。验证：新增 JSON 逐个 `ConvertFrom-Json` 通过；`vue-tsc --noEmit` 通过。 |
| P2 | Approval UX upgrade | done | 参考 Codex 审批卡，前端显示工具/命令、工作区、风险原因、Allow 下拉范围、Skip；后端审批 hydration 补充 `projectPath`、`workspaceBasePath`、`approvalKey`，审批 scope 继续复用现有机制。自动审批配置入口保留为后续策略页增强，不阻塞当前 Teacher 资料读取审批闭环。 | 用户清楚知道批准什么、批准到什么范围；拒绝后能看到替代路径。验证：`vue-tsc --noEmit` passed；ApprovalService、ChatController targeted `javac` passed。 |
| P3 | Harness scoring v2 | done | 将 Teacher v2 规则接入 mock acceptance/scoring：新增 `TeacherAcceptanceService`，从 RulePack 识别业务模块、题型规则、分值标签、材料/来源要求、答案采分、开放题方向、文言翻译采分、现代文来源真实性和目录扫描控制；`HarnessRunService` 合并规则包 scoring signals、score adjustment 和 gate blockers；Chat 样例任务详情展示 Teacher 规则包证据。导出内容差异校验单独保留在 Export regression hardening。 | Teacher mock 任务从 heuristic 变成规则驱动验收；失败时指出具体规则项。验证：`vue-tsc --noEmit` passed；TeacherAcceptanceService / HarnessRunService targeted `javac` passed。 |
| P3 | Teacher self-improve draft loop | done | 借鉴 Hermes-style learning loop：从 Harness 失败、用户修正、导出失败和人工反馈中总结可复用经验，生成 RulePack patch、Teacher Skill patch 或 acceptance case 草案。当前已支持基于 Harness run 生成自优化草案，自动归因到 `rule_pack`、`teacher_skill`、`knowledge_context` 或 `acceptance_case`，并展示风险、主要原因、建议动作、规则补丁、Skill 步骤草案和验收用例草案；管理员可接受/拒绝，发布后只让 RulePack 覆盖生效，Skill patch 和 acceptance case 保持草案态。 | 系统能生成“建议修改项”和“草案 diff/JSON”，但不会自动发布；管理员或 workspace owner 审核通过后才影响新会话。验证：`vue-tsc --noEmit` passed；TeacherImprovementDraftService / Controller targeted `javac` passed。 |
| P3 | Export regression hardening | done | 复核 Word 导出从 `TeacherExamResultV2` 结构化分块生成；添加仅试题版/完整版内容差异验证。前端仅试题版只导出 `questions` 区块，不再回退导出命题方案；完整版必须包含试题、参考答案、采分点、来源依据，但不包含命题说明/出题说明和命题质量审核；后端 `/teacher-export` 根据 `mode`、`sectionKeys` 和 Markdown 标题做二次校验，阻断无结构化内容、完整版缺区块、仅试题版混入答案/采分点/来源/审核。 | Word 导出和页面展示一致；仅试题版不包含答案；完整版包含答案、采分点、来源，不包含命题说明和质量审核。验证：`vue-tsc --noEmit` passed；ConversationController / DocxExportService targeted `javac` passed。 |
| P3 | Teacher v2 regression fixes | done | 修复 Teacher v2 内测发现的回归：工具审批上下文按 requester username 回查账号角色，避免 admin 被误判为普通用户；Teacher 分块展示在流式生成和审批等待期间保持已生成区块，不再被更少区块或审批中间态覆盖，并显示“正在生成参考答案/采分点/来源”等当前阶段提示；2026-05-26 追加修复分块来源选择，避免优先读取 plan stepResults 的半成品导致试题/答案只显示一部分；放宽“命题质量审核”等标题变体解析；过滤流式 chunk 中 promptTokens=0、completionTokens=0 的空 usage debug 日志，usage null 降到 TRACE。 | admin 不再因为 requesterId 非数字而触发“普通用户敏感工具默认需审批”；流式生成中展开/收起答案不会导致已生成试题/答案丢失；最终展示优先选择区块更完整、内容更长的候选结果，质量审核不再因标题变体丢失；空 usage chunk 不再刷 DEBUG 日志。验证：`vue-tsc --noEmit` passed；`git diff --check` passed；ToolExecutionExecutor / ToolPolicyResolver targeted `javac` passed；当前环境无 `mvn`，NodeStreamingChatHelper 单文件 `javac` 受既有 `AssistantMessage.builder()` classpath 解析问题阻断，非本次修改行。 |
| P3 | Approval denial fallback and admin role propagation | done | 2026-05-26 追加修复 Teacher plan 模式审批回归：Plan `StepExecutionNode` 正常工具执行路径会把 requesterId 传空，导致 ToolExecutionExecutor 虽已支持用户名查 admin，但仍拿不到当前用户；现在执行器入口统一以 `ChatOrigin.requesterId` 兜底，并兼容 `ROLE_ADMIN` / `global_admin` 角色命名。拒绝 `recall_structured` 等可选工具后，不再只写入“用户拒绝执行工具”并结束，而是在“不再调用该工具”的约束下继续生成基于现有上下文的替代回答。 | admin 在 Teacher plan 模式下不应再因为 requesterId 空值被判为普通用户；用户拒绝记忆检索后，Agent 应继续回答文言文/现代文/名著出题请求，并说明未使用该工具或给出替代路径。验证：ToolExecutionExecutor / ToolPolicyResolver / ChatController targeted `javac` passed；`git diff --check` passed；Teacher 手工回归待测试环境复测。 |
| P3 | Teacher module display parity | done | 2026-05-27 追加加固：文言文、现代文、名著正式出题统一使用 Teacher v2 交付协议，不允许各模块输出不同 UI 格式。后端确认出题 prompt 明确所有业务模块都必须输出 `teacher_exam_result_v2` 或固定 Markdown 分块；文言文/现代文原文材料放入 `questions` 区块，不另起非标准主区块。前端分块解析补充 `文言文试题`、`现代文试题`、`试题与材料` 等标题别名。 | 文言文试题应和名著一样按“试题/参考答案/采分点/来源依据”展示，命题说明/质量审核在内部折叠区；各块可复制、保存和导出 Word。验证：`vue-tsc --noEmit` passed；StateGraphPlanExecuteAgent targeted `javac` passed；Teacher 手工回归待测试环境复测。 |
| P4 | Later education modules | deferred | 基础知识、古诗词、协作、组卷等模块等待规则输入后按同一 RulePack 结构扩展。 | 后续新增模块不会重复 Teacher v1 的 prompt-only 问题。 |

### 名著 15 点验收矩阵

| # | Risk | v2 rule |
| --- | --- | --- |
| 1 | 名著范围/教材版本错误 | 规则包声明版本范围，超范围必须提示确认或要求补充材料。 |
| 2 | 连续会话上下文串扰 | 每次出题生成独立 `TeacherTurnContext`，仅继承用户明确保留的约束。 |
| 3 | 主观题缺少参考答案 | 主观题 `answers` 必填，缺失即 gate fail。 |
| 4 | 材料题缺少原文/材料 | 材料题必须包含材料或来源引用；没有材料时先请求补充。 |
| 5 | 题型比例失衡 | 默认比例：填空 10%、选择 20%、简答 15%、分析 30%、探究 25%；微写作只作附加。 |
| 6 | 分值不符合阅卷习惯 | 填空/表格每空 1-2 分，选择 2 分，简答 3-4 分，分析/探究 4-8 分。 |
| 7 | 选择题形式单一 | 支持排序、匹配、比较、辨析、情节理解等多种选择题设问。 |
| 8 | 微写作偏作文训练 | 微写作只考名著理解和阅读体验，不扩大成写作能力考查。 |
| 9 | 填空/表格书写量过大 | 控制空格数和每空字数，必须有示例或明确作答格式。 |
| 10 | 辩论/开放题缺答案 | 必须给参考方向和采分点；教材有答案点时优先采用。 |
| 11 | 批注题方向不明确 | 必须指定批注角度，如人物、情节、语言、主题、写法。 |
| 12 | 超纲术语/学术化 | 禁止默认使用超纲术语；如必须使用需解释并降阶表达。 |
| 13 | UI 主结果混入命题说明 | 主结果优先展示试题；命题说明/质量审核折叠或内部化。 |
| 14 | 插图问题 | 本轮不默认生成插图；涉及插图时要求用户上传或确认素材来源。 |
| 15 | 其他模块缺失 | 文言文、现代文按独立 RulePack 扩展，不混用名著规则。 |

### Confirmation Gate

Before implementing feature code for v2, confirm this staged order:

1. P0 context/state/structured contract first.
2. P1 名著规则包、上下文扫描优化、RulePack + Teacher Skill foundation second.
3. P2 文言文/现代文规则包和审批 UX third.
4. P3 Harness scoring、self-improve draft loop、export regression fourth.

## P2 Attachment and Large Context Backlog

| Task | Status | Reference | Acceptance |
| --- | --- | --- | --- |
| Image chat optimization | in_progress | `https://github.com/lintsinghua/claude-code-book` multimodal/context sections and current Chat attachment code. | Images enter the same Harness/context pipeline with preview, metadata, size limits, and model capability checks. Current landing: uploaded image attachments now carry a context strategy hint so users and prompts know when they enter multimodal context directly or fall back to image summarization. |
| File attachment routing | in_progress | Agent Harness context/tool-loop patterns plus existing Wiki raw material ingestion. | Small files can be summarized inline; large files are indexed or routed through Wiki/Context Router instead of flooding prompt context. Current landing: chat uploads now classify attachments as image direct, document extraction, text reference, file reference, or large-index-recommended; the hint is persisted in message content parts and shown in the composer. |
| Large attachment context budget | pending | Context compression, memory, and tool-result compaction patterns from the reference repo. | Context Router chooses summaries/chunks by query relevance and budget, with observable source attribution. |

## Local Agent 本地能力优先实施计划

| Step | Status | Scope | Core landing | Acceptance |
| --- | --- | --- | --- | --- |
| Step 1 - Boundary model and topology | done | Freeze the local-agent responsibility split across Desktop, Server, and remote model. | Canonical topology and runtime contract are now documented in `docs/local-agent-architecture.md`: Desktop executes local file/git/search/run and lightweight project indexing; Server stores workspace policy, approvals, run state, and audit; remote model only sees routed summaries/snippets/tool results instead of full repo sync. | The architecture note now explicitly states that coding scenarios must follow `local execute + minimal context bridge`, and later local-agent work must reuse this split instead of inventing ad-hoc bridge behavior. |
| Step 2 - Desktop local tool host | in_progress | Add a desktop-side local tool host for workspace-scoped file, grep, glob, git status/diff, and command execution. | A desktop-only readonly subset has landed in `mateclaw-desktop/electron/localToolHost.cjs` and is exposed through Electron IPC/preload for workspace tree, glob, grep, snippet reads, `git status`, `git diff`, and a conservative readonly `command.run` subset (`--version` / `-v` style environment inspection only) with workspace-root enforcement. A first writable landing also exists as `workspace.write_patch`, restricted to workspace-scoped file edits with explicit operations and expected-hash concurrency checks. An initial approval-aware command flow exists as `prepare -> approve -> deny -> execute` for a narrow validation-command allowlist using short-lived desktop approval tokens, so canceling a pending local validation request now also persists back into shared Harness approval state instead of only clearing renderer memory. Each desktop-local tool result now also emits a normalized `desktop-local-tool-result.v1` payload, the server now exposes a Harness ingest endpoint that maps this payload into shared Harness tool invocation / approval state, Electron exposes a bridge method to post these payloads back to the server with optional auth/workspace headers, the renderer has typed desktop wrapper helpers that can auto-forward `sharedPayload` into Harness when a `harnessRunId` is available, Chat's desktop project panel now uses those wrappers by default to capture local workspace tree + git evidence when an active Harness run exists, and that default git evidence path now ingests a bounded local `git diff` snapshot whenever the working tree is not clean. Desktop directory browsing in Chat now also uses the same wrapper path by default instead of the server-side directory listing route, loading desktop project insight now auto-reads a small key-file snippet set (`AGENTS.md` / `README.md` / `package.json` / `pom.xml` / `Dockerfile` as available) into Harness, the same panel now also emits a query-aware local `workspace.grep` evidence pass based on the latest user task, and it now adds a bounded readonly local toolchain inspection pass (`node --version`, `pnpm --version`, `java -version`, `mvn -v` as relevant) based on project hints, so the first default chain is moving toward `tree -> git status -> git diff -> readonly toolchain -> grep -> snippet`. Both reply-level review metadata plus Project panel file summaries now understand `workspace.write_patch` results so desktop-local patch writes immediately surface as changed-file evidence, the Project panel command hints can now launch the approval-aware desktop validation path directly from UI for eligible local test/lint/build commands with pending local validation state rehydrated from Harness after refresh or panel reopen, the latest assistant review panel now also rehydrates compact local validation results so test/lint/build evidence shows up alongside changed files in message-level review, and the Project panel's latest-reply/history cards now also carry that validation evidence instead of limiting summary surfaces to file-only deltas. The desktop shell proxy path has also been hardened for hosted-backend packaging: proxied `/api/*` requests now drop browser `Origin` / `Referer` and forwarded headers that previously caused remote hosted deployments to reject login with `Invalid CORS request`, while packaged desktop config loading now migrates stale local/legacy defaults toward the current packaged backend target. | The app can already inspect local workspace structure, search results, snippets, readonly git state, bounded diff evidence, bounded readonly toolchain evidence, perform narrow workspace-scoped patch writes, gate a small set of validation commands behind an explicit desktop approval step, emit a shared evidence payload, and has server / desktop / renderer / Chat bridge pieces ready for landing that payload into Harness state without asking the remote service to ingest the whole directory. The packaged desktop login regression against hosted backends has now been root-caused, fixed, locally reproduced/validated through the shell proxy, and rebuilt into the latest portable package. Step 2 completes after broader desktop-local read/write/approval flows also switch to these wrappers by default and approvals/audit persistence are fully unified. |
| Step 3 - Project map and context reduction | pending | Build the project-aware reduction path used before any remote reasoning turn. | Desktop bridge returns repo map, key files, changed files, symbol/search hits, and targeted snippets first; only escalates to deeper reads when needed; no blanket recursive file-content upload. | For a local coding task, first-turn context contains project summary + selected files/snippets + recent diffs, and repeated turns reuse cache instead of rescanning the whole project. |
| Step 4 - Approval, sandbox, and trust levels | pending | Unify desktop local execution with existing workspace policy and approval UX. | Risky local operations (write outside root, destructive commands, broad shell/network actions, hidden-path reads) surface the same approval model and policy wording as server-side tools. | Users can distinguish read-only local inspection from mutating/high-risk actions, and policy denials remain explainable with clear recovery hints. |
| Step 5 - Remote orchestration protocol | pending | Define the bridge payloads between Desktop local host, Server harness state, and remote model. | Standardize payload classes for project summary, selected snippets, command/test output, diff summary, and approval findings so the model consumes compact structured context instead of raw repo snapshots. | Chat/harness runs can replay local-tool traces and audits from structured payloads, and transport cost stays bounded by task-relevant evidence rather than repository size. |
| Step 6 - Coding acceptance path | pending | Validate the local-agent loop end to end on the built-in Coding Agent scenario. | Use a real local workspace task to prove `locate project -> shortlist files -> read snippets -> patch -> run validation -> summarize diff/test evidence`, all through the desktop-local bridge. | The Coding Agent sample task completes without full-repo upload, with observable approvals, changed-file evidence, validation output, and final summary linked into HarnessRun / Chat surfaces. |

### Local Agent implementation guardrails

- Never bridge full local directory contents to remote reasoning by default.
- Prefer `list/search/index/snippet` over recursive full-file reads.
- Keep local execution authority in Desktop/App; keep audit/policy/run-state authority in Server/Harness.
- Reuse Phase 4 workspace policy and Phase 5 project cache/router instead of introducing a disconnected local-agent stack.
- Treat skills/MCP as optional capability extensions, not as the primary substrate for core local coding workflows.

## Status Update Rules

- Before starting a phase, set its status to `in_progress` and record expected modules.
- After completing a phase, set its status to `done`, list actual modified files, record tests, and capture residual risks.
- If blocked, set status to `blocked` with reason and recommended path.
- Before moving to the next phase, check `git status` and confirm unrelated files were not changed.
